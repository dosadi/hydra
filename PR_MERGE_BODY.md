Auto-merge PR: merge/sim-into-main branch -> main

This PR contains an attempted merge of `sim/cleanup-docker` into `main`.
Merge conflicts were detected and intentionally left in the branch for review.

Please inspect the files with conflict markers and resolve them in this branch.

Conflicted files (need attention):

- (no conflicted files found)

Notes:
- Branch: $MERGE_BRANCH
- Created automatically by integration agent to surface merge conflicts for human review.

Suggested resolution strategy:
- RTL (`/rtl/**`) and new integration scripts (`/scripts/**`): prefer `sim/cleanup-docker` changes where appropriate.
- Docs and repo governance files (`/docs/**`, `.github/**`): review carefully; prefer `main` unless the integration package is required.

After resolving conflicts, commit the resolved files and push to this branch; then mark the PR ready for merge.
