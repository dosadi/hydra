# Minimal kmod Makefile placeholder for Hydra PCI stub on FreeBSD.
# Adjust KMOD/CFLAGS/SRCS when wiring BAR mapping and IOCTLs.

KMOD    = hydra
SRCS    = hydra_pci_stub.c device_if.h bus_if.h pci_if.h
CFLAGS += -I${.CURDIR}/../linux/uapi

.include <bsd.kmod.mk>
