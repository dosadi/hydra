// viewer_main.cpp
// Thin wrapper that includes the canonical harness implementation.
// We include `live_sdl_main.cpp` so the authoritative harness lives in one place
// (avoids accidental duplication and keeps build simple).

#include "live_sdl_main.cpp"
